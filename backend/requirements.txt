# Core Flask backend dependencies
flask
flask-cors
requests
wikipedia
transformers
scikit-learn
python-dotenv
gunicorn

# ✅ Pin NumPy to a compatible version
numpy<2

# ✅ CPU-only PyTorch (from official PyTorch CPU index)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0
